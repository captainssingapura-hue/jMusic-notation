package music.notation.phrase;

/**
 * Strategy for interpolating tempo between two BPM values
 * during a gradual tempo transition (accelerando/ritardando).
 */
public enum TransitionMethod {
    LINEAR
}
